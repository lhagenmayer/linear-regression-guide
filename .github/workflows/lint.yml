name: Code Quality Checks

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-dev.txt

    - name: Check code formatting with Black
      run: |
        python -m black --check --diff src/*.py tests/*.py run.py scripts/*.sh

    - name: Lint with Flake8
      run: |
        python -m flake8 src/*.py tests/*.py run.py --statistics

    - name: Type check with MyPy
      # MyPy is run but doesn't fail the build
      continue-on-error: true
      run: |
        python -m mypy src/app.py src/config.py src/data.py src/plots.py src/content.py src/logger.py src/accessibility.py
